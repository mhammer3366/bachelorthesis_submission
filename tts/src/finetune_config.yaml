# --- ModelArguments ---
local_model_dir: /home/chatterbox/kartoffelbox_model

# --- DataArguments ---
metadata_file: /home/Dataset/STT4SG-350/updated_train_all.tsv
text_column_name: sentence
audio_column_name: path
eval_split_size: 0.0005

# --- CustomTrainingArguments (inherits from TrainingArguments) ---
output_dir: ./checkpoints/chatterbox_finetuned_stt_all
num_train_epochs: 1
per_device_train_batch_size: 4
gradient_accumulation_steps: 2
learning_rate: 5e-5
warmup_steps: 100
logging_steps: 10
evaluation_strategy: steps
eval_steps: 2000
save_strategy: steps
save_steps: 4000
save_total_limit: 4
max_steps: 1
fp16: true
report_to: tensorboard
dataloader_num_workers: 8
do_train: true
do_eval: true
dataloader_pin_memory: false
label_names:
  - labels_speech